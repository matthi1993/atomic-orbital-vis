import { css } from 'lit';

export const typography = css`
  :host {
    --font-family: 'Segoe UI', system-ui, sans-serif;
    --fs-xs: 10px;
    --fs-sm: 12px;
    --fs-md: 14px;
    --fs-lg: 16px;
    --fw-semibold: 600;
    --fw-bold: 700;
  }
`;
