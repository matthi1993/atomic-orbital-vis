import type { OrbitalParams } from '../types.js';

export const DEFAULT_PARAMS: OrbitalParams = {
  n: 2,
  l: 1,
  m: 0,
  count: 10000,
  threshold: 0.15,
  scale: 10.0,
  pointSize: 4.0,
  rotSpeed: 0,
};
