import { css } from 'lit';

export const animations = css`
  :host {
    --ease-default: ease;
    --transition-fast: 0.12s;
    --transition-base: 0.15s;
    --transition-slow: 0.2s;
    --transition-slower: 0.25s;
    --transition-slowest: 0.3s;
  }
`;
